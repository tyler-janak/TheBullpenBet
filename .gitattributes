# No Git LFS. The runtime data/*.csv files (≤16 MB each) are committed as
# normal git objects. The large pitch-level Statcast caches
# (pitch_data_2025.csv / pitch_data_2026.csv / pitch_data_combined.csv) are
# gitignored and regenerated by refresh_full_history.py — they are never
# stored in the repo.
* text=auto
